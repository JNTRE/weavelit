[package]
name = "weavelit-server-database"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dependencies]
getrandom = { version = "=0.4.3", default-features = false }
subtle.workspace = true
weavelit-server-database-authority = { path = "../weavelit-server-database-authority" }
