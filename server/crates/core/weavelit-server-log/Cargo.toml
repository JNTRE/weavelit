[package]
name = "weavelit-server-log"
version = "0.1.0"
edition = "2024"
publish = false

[lib]
path = "src/lib.rs"

[dev-dependencies]
serde_json = { version = "=1.0.151", default-features = false, features = ["std"] }