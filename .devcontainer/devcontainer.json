{
  "name": "Weavelit Server Development",
  "build": {
    "dockerfile": "../server/containers/dev/Containerfile",
    "context": ".."
  },
  "workspaceFolder": "/workspace",
  "workspaceMount": "source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached",
  "remoteUser": "weavelit",
  "containerEnv": {
    "WEAVELIT_STATE_ROOT": "/var/lib/weavelit/state-root"
  },
  "mounts": [
    "source=weavelit-server-state-root,target=/var/lib/weavelit/state-root,type=volume",
    "source=weavelit-gh-cli-config,target=/home/weavelit/.config/gh,type=volume"
  ],
  "initializeCommand": "owner=10001:10001; if [ \"$(uname)\" = Linux ]; then owner=$(id -u):$(id -g); fi; docker volume create weavelit-server-state-root >/dev/null && docker volume create weavelit-gh-cli-config >/dev/null && docker run --rm -v weavelit-server-state-root:/state -v weavelit-gh-cli-config:/gh ubuntu:26.04@sha256:3131b4cc82a783df6c9df078f86e01819a13594b865c2cad47bd1bca2b7063bb sh -c \"chown $owner /state /gh && chmod 700 /state /gh\"",
  "customizations": {
    "vscode": {
      "extensions": [
        "rust-lang.rust-analyzer",
        "ms-vscode-remote.remote-containers"
      ]
    }
  }
}
